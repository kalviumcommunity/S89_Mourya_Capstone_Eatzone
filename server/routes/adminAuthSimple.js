import express from "express";

const adminAuthRouter = express.Router();

// Admin authentication removed - direct access enabled
// No login or verification endpoints needed

export default adminAuthRouter;
